buildscript {
    repositories {
        mavenCentral()
        jcenter()
    }

    dependencies {
        classpath 'com.android.tools.build:gradle:2.1.0-alpha1'
    }
}

task wrapper(type: Wrapper) {
    gradleVersion = '2.2.1'
}

allprojects {
    group = 'com.unisontech'
    version = '3.2.0'

    repositories {
        mavenCentral()
    }

    /*tasks.withType(Compile) {
        options.encoding = "UTF-8"
    }*/
}
